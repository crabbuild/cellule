# Runtime contracts

The SQL and peer contracts in [contracts](contracts/) are checked alongside the runtime implementation. See the workspace [architecture](../../../docs/architecture.md) for crate boundaries and the [reference application](../../cellule-app/tests/reference_application.rs) for working examples.

The original Crab-specific design notes remain in the Crab repository. Cellule keeps the reusable runtime mechanics and their executable contract fixtures here.
